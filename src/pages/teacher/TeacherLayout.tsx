import {useEffect,useState} from "react";import TeacherHeader from "./TeacherHeader";import TeacherSidebar from "./TeacherSidebar";import TeacherHome from "./TeacherHome";import TeacherDailyLogPage from "../../domains/teacherIntelligence/pages/TeacherDailyLogPage";import TeachingJournalPage from "../../domains/teacherIntelligence/pages/TeachingJournalPage";import MyClassroomPage from "../../domains/teacherIntelligence/pages/MyClassroomPage";import ExamPreparationPage from "../../domains/teacherIntelligence/pages/ExamPreparationPage";import {getCurrentTeacher} from "../../services/identityService";import {getSchoolFeatureKeys,TEACHER_FEATURES} from "../../data/schoolFeatureAccessRepository";
interface Props{onLogout:()=>void;}export default function TeacherLayout({onLogout}:Props){const[activePage,setActivePage]=useState("dashboard");const[enabled,setEnabled]=useState<string[]|null>(null);useEffect(()=>{void(async()=>{const t=getCurrentTeacher();if(!t?.schoolUuid){setEnabled(TEACHER_FEATURES.map(x=>x.key));return;}const keys=await getSchoolFeatureKeys(t.schoolUuid,"teacher");setEnabled(keys);if(keys.length&&!keys.includes(activePage))setActivePage(keys[0]);})();},[]);if(enabled===null)return null;return <div style={{display:"flex",minHeight:"100vh",background:"#F5F6FA"}}><TeacherSidebar activePage={activePage} onNavigate={setActivePage} enabledPages={enabled}/><div style={{flex:1,display:"flex",flexDirection:"column",overflow:"hidden"}}><TeacherHeader onLogout={onLogout}/><div style={{flex:1,overflowY:"auto"}}>{enabled.length===0&&<div style={{padding:32}}>No Teacher Portal modules are enabled for this school.</div>}{enabled.includes(activePage)&&activePage==="dashboard"&&<TeacherHome/>}{enabled.includes(activePage)&&activePage==="daily-log"&&<TeacherDailyLogPage/>}{enabled.includes(activePage)&&activePage==="teaching-journal"&&<TeachingJournalPage/>}{enabled.includes(activePage)&&activePage==="my-classroom"&&<MyClassroomPage/>}{enabled.includes(activePage)&&activePage==="exam-preparation"&&<ExamPreparationPage/>}</div></div></div>;}
